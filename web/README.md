# cube20 dashboard

HeroUI + React dashboard for the local `cube` binary.

```sh
npm install
npm run build
```

The Go server embeds `web/dist`, so rebuild the dashboard before building the `cube` binary when frontend files change.
